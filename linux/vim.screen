#
# Initiate a screen session with a nice 
# customized development environment set up
#
# Run this file from console with `screen -c ~/vim.screen`
# (this runs the screen application from the arg config file instead of .screenrc)
#

source ~/.screenrc

screen -t dev1 0
split
#split -v
#focus right
#screen -t dev2
focus down
screen -t console
resize 15
focus up
